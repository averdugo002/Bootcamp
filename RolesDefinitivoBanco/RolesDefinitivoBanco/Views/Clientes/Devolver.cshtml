@model RolesDefinitivoBanco.Models.Cliente

@{
    ViewData["Title"] = "Devolución de dinero";
}

<h1>Edit</h1>

<h4> @ViewData["Title"]</h4>
@*<h4>@ViewData["Title"]</h4>
<partial name="_StatusMessage" model="Model.StatusMessage" />*@
<hr />
<div class="row">
    <div class="col-md-4">
        @Html.Raw(TempData["msg"])
        <p> @Model.Balance </p>

        <form asp-action="Devolver">

            <div class="form-group">
                <label class="control-label">Introduzca la cantidad a retirar: </label>
                <input name="devolucionDinero" class="form-control" />
            </div>

            <div class="form-group">

                <input type="submit" value="Save" class="btn btn-primary" />

            </div>



        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

}
